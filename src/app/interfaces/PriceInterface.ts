export interface Price {
    _id: string;         
    id: string;           
    title: string;       
    price: number;        
    updateAllow: boolean; 
    owner: string;       
    createdAt: string;    
    updatedAt: string;   
}
